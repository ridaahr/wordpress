// Usamos la lcase de Slicknav para añadir al proyecto y darle estilos y funcionalidades propias
jQuery(document).ready( $ => {
    $('.site-header .menu-principal .menu').slicknav({
        label: '',
  
        // si queremos que el boton baje 
        //appendTo: '.site-header'
    });

});
